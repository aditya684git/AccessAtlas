# Backend Requirements for AccessAtlas
pip>=23.1
## Core Dependencies
fastapi==0.104.1          # Web framework
uvicorn==0.24.0           # ASGI server
python-multipart==0.0.6   # File upload support

## ML/CV
ultralytics==8.0.200      # YOLOv5 implementation
torch==2.1.0              # PyTorch (CPU version, use torch.org for GPU)
torchvision==0.16.0       # Computer vision utilities
numpy>=1.24.0,<2.0.0      # NumPy (required by torch/ultralytics)
pillow==10.1.0            # Image processing

## Voice & Accessibility
pyttsx3==2.90             # Text-to-speech (Windows, macOS, Linux support)

## Optional: Development
pytest==7.4.3             # Testing framework
black==23.12.0            # Code formatting
pylint==3.0.3             # Linting
python-dotenv==1.0.0      # Environment variables

## Notes
- torch can be optimized for your GPU: see https://pytorch.org/get-started/locally
- pyttsx3 requires no internet and works offline
- For production, consider using Gunicorn instead of uvicorn
